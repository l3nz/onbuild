(ns lenz.onbuild.core-test
      (:require [clojure.test :refer :all]
                [lenz.onbuild.core :refer :all]))

  (deftest a-test
           (testing "FIXME, I fail."
             (is (= 0 1))))
